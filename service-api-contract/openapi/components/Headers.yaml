components:
  parameters:
    x-correlation-id:
      name: x-correlation-id
      description: UUID to identify a specific request in case of an issue.
      in: header
      required: true
      schema:
        type: string
        format: uuid
